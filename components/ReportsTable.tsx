"use client";

import { useMemo, useState } from "react";
import { jsPDF } from "jspdf";
import autoTable from "jspdf-autotable";
import { computeFeeAmount, computeNetFare, formatBRL, formatDateBR, hojeBrasilia, toNumber, type DailyEntry } from "@/lib/utils";

type Props = { entries: DailyEntry[]; initialFrom: string; initialTo: string };
type Categories = { gas: boolean; alcohol: boolean; maintenance: boolean; extras: boolean };
const DEFAULT_CATEGORIES: Categories = { gas: true, alcohol: true, maintenance: true, extras: true };

function formatKm(value: number) { return value.toLocaleString("pt-BR", { maximumFractionDigits: 0 }); }
function formatLiters(value: number) { return value.toLocaleString("pt-BR", { minimumFractionDigits: 2, maximumFractionDigits: 2 }); }
function formatHours(value: number) { return value.toLocaleString("pt-BR", { minimumFractionDigits: 0, maximumFractionDigits: 1 }); }
function formatKmPerLiter(value: number | null) { return value === null ? "—" : `${value.toLocaleString("pt-BR", { minimumFractionDigits: 2, maximumFractionDigits: 2 })} km/L`; }
function maintenanceTotal(e: DailyEntry) { const d=(e.maintenance_details||[]).reduce((s,i)=>s+toNumber(i.value),0); return d>0?d:Number(e.maintenance_expense||0); }
function extrasTotal(e: DailyEntry) { return (e.extra_expenses||[]).reduce((s,i)=>s+toNumber(i.value),0); }
function maintenanceDetails(e: DailyEntry) { return (e.maintenance_details||[]).filter(i=>toNumber(i.value)!==0||i.description).map(i=>`${formatBRL(toNumber(i.value))} — ${i.description||"Item sem descrição"}`); }
function extrasDetails(e: DailyEntry) { return (e.extra_expenses||[]).filter(i=>toNumber(i.value)!==0||i.name).map(i=>`${formatBRL(toNumber(i.value))} — ${i.name||"Item sem descrição"}`); }
function startOfWeekISO(dateISO:string){const d=new Date(`${dateISO}T12:00:00`);const day=d.getDay();const diff=day===0?-6:1-day;d.setDate(d.getDate()+diff);return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,"0")}-${String(d.getDate()).padStart(2,"0")}`;}

function DetailList({items,total,emptyLabel}:{items:string[];total:number;emptyLabel:string}) {
  return <div className="min-w-[170px] text-right">{items.length ? <div className="space-y-1 text-xs text-slate-600">{items.map((x,i)=><div key={`${x}-${i}`}>{x}</div>)}</div> : <div className="text-xs text-slate-400">{emptyLabel}</div>}<div className="mt-2 border-t border-slate-200 pt-1 font-semibold">Total: {formatBRL(total)}</div></div>;
}

export default function ReportsTable({entries,initialFrom,initialTo}:Props) {
  const [from,setFrom]=useState(initialFrom); const [to,setTo]=useState(initialTo); const [cats,setCats]=useState<Categories>(DEFAULT_CATEGORIES);
  const today=hojeBrasilia(); const [year,month]=today.split("-"); const monthStart=`${year}-${month}-01`; const weekStart=startOfWeekISO(today);
  const filtered=useMemo(()=>entries.filter(e=>e.date>=from&&e.date<=to),[entries,from,to]);
  const monthToDate=useMemo(()=>entries.filter(e=>e.date>=monthStart&&e.date<=today),[entries,monthStart,today]);

  const values=(e:DailyEntry)=>{
    const gross=Number(e.gross_amount??0)||0, feePercent=Number(e.fee_percent??0)||0;
    const gas=cats.gas?Math.max(0,Number(e.gas_expense)||0):0;
    const alcohol=cats.alcohol?Math.max(0,Number(e.alcohol_expense)||0):0;
    const maintenance=cats.maintenance?maintenanceTotal(e):0, extras=cats.extras?extrasTotal(e):0;
    const km=Math.max(0,Number(e.km_driven)||0);
    const hours=Math.max(0,Number(e.hours_worked)||0);
    const liters=Math.max(0,Number(e.gasoline_liters)||0)+Math.max(0,Number(e.alcohol_liters)||0);
    const fuel=Math.max(0,Number(e.gas_expense)||0)+Math.max(0,Number(e.alcohol_expense)||0);
    const net=computeNetFare(e), profit=net-fuel-maintenance-extras;
    return {gross,feePercent,feeAmount:computeFeeAmount(e),net,gas,alcohol,maintenance,extras,profit,km,hours,liters,fuel,kmPerLiter:liters>0?km/liters:null};
  };

  const totals=useMemo(()=>filtered.reduce((a,e)=>{const v=values(e); for(const k of ["gross","feeAmount","net","gas","alcohol","maintenance","extras","profit","km","hours","liters","fuel"] as const)a[k]+=v[k]; return a;},{gross:0,feeAmount:0,net:0,gas:0,alcohol:0,maintenance:0,extras:0,profit:0,km:0,hours:0,liters:0,fuel:0}),[filtered,cats]);
  const monthKm=useMemo(()=>monthToDate.reduce((s,e)=>s+Math.max(0,Number(e.km_driven)||0),0),[monthToDate]);
  const totalKmPerLiter=totals.liters>0?totals.km/totals.liters:null;
  const todayHours=useMemo(()=>filtered.filter(e=>e.date===today).reduce((s,e)=>s+Math.max(0,Number(e.hours_worked)||0),0),[filtered,today]);
  const weekHours=useMemo(()=>filtered.filter(e=>e.date>=weekStart&&e.date<=today).reduce((s,e)=>s+Math.max(0,Number(e.hours_worked)||0),0),[filtered,weekStart,today]);
  const monthHours=useMemo(()=>filtered.filter(e=>e.date>=monthStart&&e.date<=today).reduce((s,e)=>s+Math.max(0,Number(e.hours_worked)||0),0),[filtered,monthStart,today]);
  const hoursByDate=useMemo(()=>{const map=new Map<string,number>();for(const e of filtered){const h=Math.max(0,Number(e.hours_worked)||0);map.set(e.date,(map.get(e.date)||0)+h);}return Array.from(map.entries()).sort((a,b)=>a[0].localeCompare(b[0]));},[filtered]);

  const summaryText=useMemo(()=>[
    `Relatório FaturApp — ${formatDateBR(from)} a ${formatDateBR(to)}`,
    `Horas trabalhadas no período: ${formatHours(totals.hours)} h`,
    `Horas hoje: ${formatHours(todayHours)} h`,
    `Horas nesta semana: ${formatHours(weekHours)} h`,
    `Horas neste mês: ${formatHours(monthHours)} h`,
    `Km no período: ${formatKm(totals.km)} km`,
    `Km no mês até hoje: ${formatKm(monthKm)} km`,
    `Gasolina: ${formatBRL(totals.gas)}`,
    `Álcool: ${formatBRL(totals.alcohol)}`,
    `Combustível total: ${formatBRL(totals.fuel)}`,
    `Litros: ${formatLiters(totals.liters)} L`,
    `Consumo médio: ${formatKmPerLiter(totalKmPerLiter)}`,
    `Receita líquida: ${formatBRL(totals.net)}`,
    `Lucro total: ${formatBRL(totals.profit)}`,
    ...(cats.maintenance?[`Manutenção: ${formatBRL(totals.maintenance)}`]:[]),
    ...(cats.extras?[`Gastos extras: ${formatBRL(totals.extras)}`]:[])
  ].join("\n"),[from,to,totals,todayHours,weekHours,monthHours,monthKm,totalKmPerLiter,cats]);

  function openWhatsApp(){window.open(`https://wa.me/?text=${encodeURIComponent(summaryText)}`,"_blank");}
  function openEmail(){window.location.href=`mailto:?subject=${encodeURIComponent("Relatório FaturApp")}&body=${encodeURIComponent(summaryText)}`;}
  function downloadPDF(){const doc=new jsPDF("landscape","mm","a4");doc.setFontSize(16);doc.text("FaturApp — Relatório",14,15);doc.setFontSize(9);doc.text(`Período: ${formatDateBR(from)} até ${formatDateBR(to)}`,14,22);doc.text(`Horas no período: ${formatHours(totals.hours)} h`,14,27);doc.text(`Horas hoje: ${formatHours(todayHours)} h`,75,27);doc.text(`Horas semana: ${formatHours(weekHours)} h`,130,27);doc.text(`Horas mês: ${formatHours(monthHours)} h`,185,27);doc.text(`Km no período: ${formatKm(totals.km)} km`,14,32);const head=["Data","Horas","Km","Gasolina","Álcool","Combustível","Bruto","Taxa","Líquida","Manutenção","Extras","Lucro"];const body=filtered.map(e=>{const v=values(e);const md=maintenanceDetails(e),ed=extrasDetails(e);return[formatDateBR(e.date),formatHours(v.hours)+" h",formatKm(v.km),formatBRL(v.gas),formatBRL(v.alcohol),formatBRL(v.fuel),v.gross>0?formatBRL(v.gross):"—",v.gross>0?`${v.feePercent.toFixed(2)}%`:"—",formatBRL(v.net),md.length?`${md.join("\n")}\nTOTAL: ${formatBRL(v.maintenance)}`:formatBRL(v.maintenance),ed.length?`${ed.join("\n")}\nTOTAL: ${formatBRL(v.extras)}`:formatBRL(v.extras),formatBRL(v.profit)]});body.push(["TOTAL",formatHours(totals.hours)+" h",formatKm(totals.km),formatBRL(totals.gas),formatBRL(totals.alcohol),formatBRL(totals.fuel),formatBRL(totals.gross),"",formatBRL(totals.net),`TOTAL: ${formatBRL(totals.maintenance)}`,`TOTAL: ${formatBRL(totals.extras)}`,formatBRL(totals.profit)]);autoTable(doc,{head:[head],body,startY:38,theme:"grid",styles:{fontSize:6,cellPadding:1.5,valign:"middle"},headStyles:{fontSize:6,fontStyle:"bold"},margin:{left:6,right:6},didParseCell:d=>{if(d.row.index===body.length-1)d.cell.styles.fontStyle="bold";}});doc.save(`FaturApp_Relatorio_${from}_${to}.pdf`);}
  const cat=(key:keyof Categories,label:string)=><label className="inline-flex cursor-pointer items-center gap-2 rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm hover:bg-slate-50"><input type="checkbox" checked={cats[key]} onChange={e=>setCats(c=>({...c,[key]:e.target.checked}))}/>{label}</label>;

  return <div className="space-y-5">
    <section className="rounded-xl border border-slate-200 bg-white p-4 shadow-sm"><h2 className="text-lg font-bold text-slate-800">Filtros do relatório</h2><p className="mt-1 text-sm text-slate-500">Selecione o período e as categorias de gastos.</p><div className="mt-4 grid grid-cols-1 gap-3 sm:grid-cols-2"><div><label className="label">Data inicial</label><input type="date" className="input" value={from} onChange={e=>setFrom(e.target.value)}/></div><div><label className="label">Data final</label><input type="date" className="input" value={to} onChange={e=>setTo(e.target.value)}/></div></div><div className="mt-4"><p className="mb-2 text-sm font-semibold text-slate-700">Categorias de gastos</p><div className="flex flex-wrap gap-2">{cat("gas","Gasolina")}{cat("alcohol","Álcool")}{cat("maintenance","Manutenção")}{cat("extras","Gastos extras")}</div></div></section>

    <section className="rounded-xl border border-brand-200 bg-brand-50 p-4 shadow-sm"><p className="text-xs font-bold uppercase tracking-wide text-brand-700">HORAS TRABALHADAS</p><h2 className="mt-1 text-lg font-bold text-slate-900">Quanto tempo você trabalhou?</h2><p className="mt-1 text-sm text-slate-600">Cada lançamento mantém suas horas individualmente. Quando houver mais de um lançamento no mesmo dia, as horas são somadas para mostrar o total daquele dia.</p><div className="mt-4 grid grid-cols-2 gap-3 lg:grid-cols-4"><div className="rounded-xl border border-slate-200 bg-white p-4"><p className="text-xs text-slate-500">Hoje</p><p className="mt-1 text-xl font-bold text-slate-900">Você trabalhou {formatHours(todayHours)} h</p></div><div className="rounded-xl border border-slate-200 bg-white p-4"><p className="text-xs text-slate-500">Nesta semana</p><p className="mt-1 text-xl font-bold text-slate-900">Você trabalhou {formatHours(weekHours)} h</p></div><div className="rounded-xl border border-slate-200 bg-white p-4"><p className="text-xs text-slate-500">Neste mês</p><p className="mt-1 text-xl font-bold text-slate-900">Você trabalhou {formatHours(monthHours)} h</p></div><div className="rounded-xl border border-slate-200 bg-white p-4"><p className="text-xs text-slate-500">Período selecionado</p><p className="mt-1 text-xl font-bold text-slate-900">Você trabalhou {formatHours(totals.hours)} h</p></div></div>{hoursByDate.length>0&&<div className="mt-4 overflow-x-auto rounded-xl border border-slate-200 bg-white"><table className="min-w-full text-sm"><thead className="bg-slate-100"><tr><th className="px-4 py-3 text-left">Dia</th><th className="px-4 py-3 text-right">Horas trabalhadas</th></tr></thead><tbody>{hoursByDate.map(([date,hours])=><tr key={date} className="border-t border-slate-200"><td className="px-4 py-3 font-medium">{formatDateBR(date)}</td><td className="px-4 py-3 text-right font-bold">Você trabalhou {formatHours(hours)} h</td></tr>)}</tbody><tfoot className="bg-slate-50 font-bold"><tr className="border-t border-slate-200"><td className="px-4 py-3">TOTAL DO PERÍODO</td><td className="px-4 py-3 text-right">Você trabalhou {formatHours(totals.hours)} h</td></tr></tfoot></table></div>}</section>

    <section className="rounded-xl border border-slate-200 bg-white p-4 shadow-sm"><div className="mb-4"><h2 className="text-lg font-bold text-slate-800">Detalhamento financeiro</h2><p className="text-sm text-slate-500">Receitas, combustível, manutenção, gastos extras e lucro. As descrições de cada gasto permanecem visíveis.</p></div><div className="grid grid-cols-2 gap-3 lg:grid-cols-4"><div className="rounded-xl border border-slate-200 bg-slate-50 p-4"><p className="text-xs text-slate-500">Receita líquida</p><p className="mt-1 text-xl font-bold">{formatBRL(totals.net)}</p></div><div className="rounded-xl border border-slate-200 bg-slate-50 p-4"><p className="text-xs text-slate-500">Litros abastecidos</p><p className="mt-1 text-xl font-bold">{formatLiters(totals.liters)} L</p></div><div className="rounded-xl border border-slate-200 bg-slate-50 p-4"><p className="text-xs text-slate-500">Estimativa km/L</p><p className="mt-1 text-xl font-bold">{formatKmPerLiter(totalKmPerLiter)}</p><p className="mt-1 text-xs text-slate-500">Km total ÷ litros abastecidos; não é consumo real.</p></div><div className="rounded-xl border border-slate-200 bg-slate-50 p-4"><p className="text-xs text-slate-500">Lucro total</p><p className="mt-1 text-xl font-bold">{formatBRL(totals.profit)}</p></div></div><div className="mt-4 overflow-x-auto rounded-xl border border-slate-200"><table className="min-w-full text-sm"><thead className="bg-slate-100"><tr><th className="px-4 py-3 text-left">Data</th><th className="px-4 py-3 text-right">Horas</th><th className="px-4 py-3 text-right">Km</th><th className="px-4 py-3 text-right">Combustível</th><th className="px-4 py-3 text-right">Receita</th>{cats.gas&&<th className="px-4 py-3 text-right">Gasolina</th>}{cats.alcohol&&<th className="px-4 py-3 text-right">Álcool</th>}{cats.maintenance&&<th className="px-4 py-3 text-right">Manutenção</th>}{cats.extras&&<th className="px-4 py-3 text-right">Gastos extras</th>}<th className="px-4 py-3 text-right">Lucro do dia</th></tr></thead><tbody>{filtered.length?filtered.map(e=>{const v=values(e);return <tr key={e.id} className="border-t border-slate-200 align-top"><td className="px-4 py-3 font-medium whitespace-nowrap">{formatDateBR(e.date)}</td><td className="px-4 py-3 text-right font-semibold whitespace-nowrap">{formatHours(v.hours)} h</td><td className="px-4 py-3 text-right font-semibold whitespace-nowrap">{formatKm(v.km)} km</td><td className="px-4 py-3 text-right whitespace-nowrap"><div className="font-medium">{formatBRL(v.fuel)}</div><div className="text-xs text-slate-500">{formatLiters(v.liters)} L · {formatKmPerLiter(v.kmPerLiter)}</div></td><td className="px-4 py-3 text-right whitespace-nowrap"><div>Bruto: {v.gross>0?formatBRL(v.gross):"—"}</div><div className="text-xs text-slate-500">Taxa: {v.gross>0?`${v.feePercent.toFixed(2)}%`:"—"}</div><div className="font-semibold">Líquida: {formatBRL(v.net)}</div></td>{cats.gas&&<td className="px-4 py-3 text-right">{formatBRL(v.gas)}</td>}{cats.alcohol&&<td className="px-4 py-3 text-right">{formatBRL(v.alcohol)}</td>}{cats.maintenance&&<td className="px-4 py-3"><DetailList items={maintenanceDetails(e)} total={v.maintenance} emptyLabel="Sem manutenção"/></td>}{cats.extras&&<td className="px-4 py-3"><DetailList items={extrasDetails(e)} total={v.extras} emptyLabel="Sem gastos extras"/></td>}<td className="px-4 py-3 text-right font-bold whitespace-nowrap">{formatBRL(v.profit)}</td></tr>}) : <tr><td colSpan={6+Number(cats.gas)+Number(cats.alcohol)+Number(cats.maintenance)+Number(cats.extras)} className="px-4 py-8 text-center text-slate-500">Nenhum lançamento no período.</td></tr>}</tbody>{filtered.length?<tfoot className="bg-slate-50 font-bold"><tr className="border-t border-slate-200"><td className="px-4 py-3">TOTAIS</td><td className="px-4 py-3 text-right">{formatHours(totals.hours)} h</td><td className="px-4 py-3 text-right">{formatKm(totals.km)} km</td><td className="px-4 py-3 text-right">{formatBRL(totals.fuel)}<div className="text-xs font-normal text-slate-500">{formatLiters(totals.liters)} L</div></td><td className="px-4 py-3 text-right">{formatBRL(totals.net)}</td>{cats.gas&&<td className="px-4 py-3 text-right">{formatBRL(totals.gas)}</td>}{cats.alcohol&&<td className="px-4 py-3 text-right">{formatBRL(totals.alcohol)}</td>}{cats.maintenance&&<td className="px-4 py-3 text-right">{formatBRL(totals.maintenance)}</td>}{cats.extras&&<td className="px-4 py-3 text-right">{formatBRL(totals.extras)}</td>}<td className="px-4 py-3 text-right">{formatBRL(totals.profit)}</td></tr></tfoot>:null}</table></div><div className="mt-4 grid grid-cols-2 gap-2 sm:flex sm:flex-wrap"><button type="button" onClick={downloadPDF} className="col-span-2 w-full rounded-lg bg-slate-800 px-3 py-2 text-sm font-semibold text-white sm:col-span-1 sm:w-auto sm:px-4">Baixar PDF</button><button type="button" onClick={openWhatsApp} className="w-full rounded-lg border border-slate-300 bg-white px-3 py-2 text-sm font-semibold sm:w-auto sm:px-4">Enviar por WhatsApp</button><button type="button" onClick={openEmail} className="w-full rounded-lg border border-slate-300 bg-white px-3 py-2 text-sm font-semibold sm:w-auto sm:px-4">Enviar por e-mail</button></div></section>
  </div>;
}
